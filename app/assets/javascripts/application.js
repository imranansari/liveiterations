require(["order!jquery",
    "order!livequery",
    "order!rails",
    "order!backbone",
    "order!modelbinding",
    "order!twDropdown",
    "order!twModal",
    'views/app'], function(AppView){
  var appView = new AppView();
});
