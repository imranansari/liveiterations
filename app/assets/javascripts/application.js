require(["order!jquery",
    "order!rails",
    "order!backbone",
    "order!modelbinding",
    "order!twDropdown",
    "order!twModal",
    'views/app'], function(AppView){
  var app_view = new AppView;
});
